defaults:
  - data: default
  - model: default
  - loss: default
  - optimizer: adamw
  - scheduler: cosine
  - optuna: default
  - _self_

seed: 42
device: auto
batch_size: 256
epochs: 50
loss:
  weighted: true
